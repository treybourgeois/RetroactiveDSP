# Recovery

If flashing fails, try the device recovery steps:

1. Disconnect and reconnect the device.
2. Boot into DFU mode (device-specific).
3. Retry flashing with a known-good stable firmware.
# Recovery

If flashing fails, try the device recovery steps:

1. Disconnect and reconnect the device.
2. Boot into DFU mode (device-specific).
3. Retry flashing with a known-good stable firmware.
# Recovery

If flashing fails, try the device recovery steps:

1. Disconnect and reconnect the device.
2. Boot into DFU mode (device-specific).
3. Retry flashing with a known-good stable firmware.
